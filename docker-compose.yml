services:
  asterisk:
    image: andrius/asterisk:latest
    container_name: asterisk
    network_mode: host
    volumes:
      - ./config:/etc/asterisk
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - NET_RAW
